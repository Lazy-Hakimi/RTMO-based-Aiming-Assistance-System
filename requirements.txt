# RTMO AimBot Enhanced 依赖
# Jetson AGX Xavier (ARM64 + CUDA 10.2/11.4) + ESP32-S3

# ========== 核心依赖 (所有模式必需) ==========
numpy>=1.21.0
opencv-python>=4.5.5
opencv-contrib-python>=4.5.5

# ESP32通信 (串口)
pyserial>=3.5

# ========== PyTorch 原生推理 (默认, --backend pytorch) ==========
# PyTorch (Jetson 版本, 需从 NVIDIA 官网下载)
# JetPack 5.1.2 + CUDA 11.4:
#   wget https://developer.download.nvidia.com/compute/redist/jp/v512/pytorch/
#     torch-2.1.0a0+41361538.nv23.06-cp38-cp38-linux_aarch64.whl
#   pip3 install torch-2.1.0a0+41361538.nv23.06-cp38-cp38-linux_aarch64.whl
#torch>=1.12.0

# MMCV (MMPose 依赖, Jetson 需从预编译 wheel 安装)
#   pip install mmcv==2.1.0 -f https://download.openmmlab.com/mmcv/dist/cu118/aarch64/index.html
#mmcv>=2.0.0

# MMPose (RTMO 模型定义与推理)
#   pip install mmpose>=1.3.0
#mmpose>=1.3.0

# MMEngine (OpenMMLab 底层引擎)
#   pip install mmengine>=0.7.0
#mmengine>=0.7.0

# ========== TensorRT 推理 (--backend tensorrt) ==========
# TensorRT (Jetson系统预装, 通过 JetPack 安装)
# tensorrt>=8.4.0
# pycuda>=2021.1   # 通过 JetPack 安装

# ONNX 转换工具 (导出/转换时使用)
# onnx>=1.12.0
# onnxsim>=0.4.0

# ========== GStreamer硬件加速 (Jetson系统预装) ==========

# ========== 开发/测试依赖 (可选) ==========
# pytest>=7.0.0
# black>=22.0.0
# pylint>=2.14.0
